.grid_2.suffix_1.filter{:class=> "filter_#{filter[0]}"}
	%h1.filter_title
		=filter[0].humanize
		%ul
			- filter[1].each do |type|
				%li.define_me
					= link_to type, intervals_path(add_params filter[0].to_sym => type)