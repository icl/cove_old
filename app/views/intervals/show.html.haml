%h3.grid_5#title
  Place Holder Title 
%div.grid_16.meta_info_container
  This is going to be the meta data

%div.grid_16#video_container
  = video_tag @interval.filename, :controls => true, :height => 400, :width => 950

%div.grid_16#tag_container
  %ul
    -if !@applied_tags.blank?
      -for tagging in @applied_tags
        %li.tag.applied
          =tagging.tag.name
    -else
      An Error with tagging occured

.grid_16#phenomenon_container
  %ul
    -if !@applied_phenomenon.blank?
      -for coding in @applied_phenomenon
        -if @phenomenon.include?(coding.code)
          %li.tag.applied
            =coding.code.name
        -else
          %li.tag.unapplied
            =coding.code.name
        
    -else
      An Error with Coding occured
     
    
